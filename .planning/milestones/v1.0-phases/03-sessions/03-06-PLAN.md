---
phase: 03-sessions
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/Chat.tsx
  - src/components/MessageList.tsx
  - src/components/MessageInput.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Scroll position is preserved when switching sessions"
    - "Draft message is preserved when switching sessions"
    - "Session state persists across page refreshes"
    - "Scroll restoration waits for messages to load before restoring position"
  artifacts:
    - path: "src/App.tsx"
      provides: "sessionState passed to Chat component"
      contains: "scrollPosition={sessionState.scrollPosition}"
    - path: "src/components/Chat.tsx"
      provides: "Props threaded to MessageList and MessageInput"
      contains: "useScrollRestoration"
    - path: "src/components/MessageList.tsx"
      provides: "Scroll restoration integration with isLoaded flag"
      contains: "isLoaded"
    - path: "src/components/MessageInput.tsx"
      provides: "Draft persistence props wired to state"
      contains: "draftMessage"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/Chat.tsx"
      via: "sessionState props"
      pattern: "scrollPosition=.*draftMessage="
    - from: "src/components/Chat.tsx"
      to: "src/hooks/useScrollRestoration.ts"
      via: "hook import and usage"
      pattern: "useScrollRestoration"
    - from: "src/components/MessageInput.tsx"
      to: "props.onDraftChange"
      via: "onChange handler"
      pattern: "onDraftChange\\("
---

<objective>
Wire orphaned session state hooks through the component tree to enable scroll and draft persistence.

Purpose: Close verification gaps where hooks exist but aren't connected to UI components.
Output: Session switching preserves scroll position and draft message; state persists across refreshes.
</objective>

<execution_context>
@/Users/callesjoenell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callesjoenell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sessions/03-VERIFICATION.md

# Existing code to wire together
@src/hooks/useSessionState.ts
@src/hooks/useScrollRestoration.ts
@src/hooks/useStreamingChat.ts
@src/App.tsx
@src/components/Chat.tsx
@src/components/MessageList.tsx
@src/components/MessageInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread session state props from App.tsx to Chat</name>
  <files>src/App.tsx, src/components/Chat.tsx</files>
  <action>
    In src/App.tsx:
    - Pass sessionState values to Chat component:
      - scrollPosition={sessionState.scrollPosition}
      - saveScrollPosition={sessionState.saveScrollPosition}
      - draftMessage={sessionState.draftMessage}
      - saveDraftMessage={sessionState.saveDraftMessage}
      - clearDraftMessage={sessionState.clearDraftMessage}

    In src/components/Chat.tsx:
    - Update ChatProps interface to accept:
      - scrollPosition: number
      - saveScrollPosition: (position: number) => void
      - draftMessage: string
      - saveDraftMessage: (draft: string) => void
      - clearDraftMessage: () => void
    - Remove useScrollIntent import and usage
    - Add import for useScrollRestoration from '../hooks/useScrollRestoration'
    - Call useScrollRestoration with:
      - sessionId: sessionId (convert to string via toString())
      - savedScrollPosition: scrollPosition prop
      - saveScrollPosition: saveScrollPosition prop
      - isLoaded: messages !== undefined (from useStreamingChat)
    - The hook returns containerRef - use this instead of containerRef from useScrollIntent
    - Keep isUserScrolledUp state locally (simple useState + scroll event check)
  </action>
  <verify>
    TypeScript compiles without errors:
    npx tsc --noEmit
  </verify>
  <done>
    Chat component receives session state props and uses useScrollRestoration hook.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate scroll restoration in MessageList</name>
  <files>src/components/Chat.tsx, src/components/MessageList.tsx</files>
  <action>
    The containerRef from useScrollRestoration in Chat.tsx is already passed to MessageList.
    MessageList already accepts containerRef prop - no changes needed to MessageList.

    However, ensure Chat.tsx:
    - Passes the containerRef from useScrollRestoration (not a separate ref)
    - Provides an onScrollChange callback that updates isUserScrolledUp state

    The scroll saving is handled by useScrollRestoration hook automatically.
    The scroll restoration happens when isLoaded becomes true.

    For isUserScrolledUp detection in Chat.tsx:
    - Add useState for isUserScrolledUp
    - Add useEffect that attaches scroll listener to containerRef
    - Check if scrollTop < scrollHeight - clientHeight - 100 (within 100px of bottom)
    - Update state on scroll
  </action>
  <verify>
    App compiles and scroll position is saved/restored on session switch:
    1. npm run dev
    2. Create messages in a session
    3. Scroll up in the message list
    4. Switch to another session
    5. Switch back - scroll position should be restored
  </verify>
  <done>
    MessageList uses scroll restoration containerRef, scroll position persists across session switches.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire draft persistence to MessageInput</name>
  <files>src/components/Chat.tsx, src/components/MessageInput.tsx</files>
  <action>
    In src/components/MessageInput.tsx:
    - Update MessageInputProps interface to add:
      - draftMessage?: string
      - onDraftChange?: (draft: string) => void
      - onSendSuccess?: () => void
    - Initialize content state with draftMessage prop (if provided)
    - Add useEffect to sync external draftMessage changes:
      ```typescript
      useEffect(() => {
        if (draftMessage !== undefined) {
          setContent(draftMessage);
        }
      }, [draftMessage]);
      ```
    - In onChange handler, call onDraftChange if provided:
      ```typescript
      onChange={(e) => {
        setContent(e.target.value);
        onDraftChange?.(e.target.value);
      }}
      ```
    - After successful send (in handleSubmit, after onSend call):
      ```typescript
      onSend(content.trim());
      setContent("");
      onSendSuccess?.();  // Clear persisted draft
      ```

    In src/components/Chat.tsx:
    - Pass draft props to MessageInput:
      - draftMessage={draftMessage}
      - onDraftChange={saveDraftMessage}
      - onSendSuccess={clearDraftMessage}
  </action>
  <verify>
    Draft persistence works:
    1. npm run dev
    2. Type a message but don't send
    3. Switch to another session
    4. Switch back - draft message should still be in input
    5. Send the message - draft should be cleared from persistence
  </verify>
  <done>
    MessageInput syncs with session draft state, drafts persist across session switches.
  </done>
</task>

</tasks>

<verification>
All gaps from 03-VERIFICATION.md are closed:

1. **Scroll position preserved**: useScrollRestoration wired in Chat, containerRef passed to MessageList
2. **Draft message preserved**: MessageInput accepts and syncs draft props
3. **Session state persists**: useSessionState in App.tsx threaded through to child components
4. **Scroll restoration race condition**: isLoaded flag computed from messages !== undefined

Run verification:
```bash
# TypeScript compiles
npx tsc --noEmit

# App runs without errors
npm run dev
```

Manual verification checklist:
- [ ] Switch sessions, scroll position restored
- [ ] Switch sessions, draft message restored
- [ ] Refresh page, scroll position and draft restored
- [ ] Send message, draft cleared from localStorage
</verification>

<success_criteria>
1. TypeScript compiles without errors
2. Switching sessions preserves scroll position
3. Switching sessions preserves draft message
4. Page refresh preserves both scroll position and draft
5. Sending a message clears the draft from localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/03-sessions/03-06-SUMMARY.md`
</output>
