---
phase: 08-conversation-design
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/hooks/useStreamingChat.ts
  - src/hooks/useCoverageState.ts
  - src/components/PhaseProgressBar.tsx
  - src/components/PhaseSegment.tsx
  - src/components/ResearchIntensityControl.tsx
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04, CONV-05]

must_haves:
  truths:
    - "useStreamingChat passes expanded context to buildSystemPrompt including coverage, energy, research state"
    - "Coverage extraction fires after every assistant response"
    - "Research trigger detection fires with every user message in ALL phases"
    - "Research triggers surface as suggestions in Claude's response (woven into prompt)"
    - "Progress bar shows sub-topic coverage within current phase"
    - "Research intensity can be changed via UI control"
    - "chatWithResearch is used for ALL phases, not just 0-2"
  artifacts:
    - path: "src/hooks/useStreamingChat.ts"
      provides: "Updated hook passing full context to system prompt and calling extraction/triggers post-turn"
    - path: "src/hooks/useCoverageState.ts"
      provides: "Hook for reading coverage state from Convex and computing progress"
      exports: ["useCoverageState"]
    - path: "src/components/PhaseProgressBar.tsx"
      provides: "Enhanced progress bar showing sub-topic coverage dots"
    - path: "src/components/ResearchIntensityControl.tsx"
      provides: "Three-level toggle for research intensity setting"
      exports: ["ResearchIntensityControl"]
  key_links:
    - from: "src/hooks/useStreamingChat.ts"
      to: "src/lib/systemPrompts.ts"
      via: "passes expanded SystemPromptContext"
      pattern: "buildSystemPrompt"
    - from: "src/hooks/useStreamingChat.ts"
      to: "convex/conversationActions.ts"
      via: "calls extractCoverage after assistant response, detectTriggers before response"
      pattern: "extractCoverage|detectTriggers"
    - from: "src/hooks/useCoverageState.ts"
      to: "convex/conversationState.ts"
      via: "queries getCoverageState"
      pattern: "getCoverageState"
    - from: "src/components/PhaseProgressBar.tsx"
      to: "src/hooks/useCoverageState.ts"
      via: "receives coverage data for sub-topic display"
      pattern: "useCoverageState"
---

<objective>
Wire the conversation design system end-to-end: update useStreamingChat to pass full context and call extraction/triggers, add coverage state hook, enhance progress bar with sub-topics, and add research intensity control.

Purpose: This is the integration plan that connects the infrastructure (Plan 01), prompt design (Plan 02), and backend actions (Plan 03) into the working UI. All CONV requirements converge here.

Output: Working end-to-end flow where conversations are state-aware, coverage tracks implicitly, research triggers in all phases, and progress bar shows sub-topic depth.
</objective>

<execution_context>
@/Users/callesjoenell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callesjoenell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-conversation-design/08-CONTEXT.md
@.planning/phases/08-conversation-design/08-01-SUMMARY.md
@.planning/phases/08-conversation-design/08-02-SUMMARY.md
@.planning/phases/08-conversation-design/08-03-SUMMARY.md
@src/hooks/useStreamingChat.ts
@src/components/PhaseProgressBar.tsx
@src/components/PhaseSegment.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useStreamingChat with expanded context + post-turn extraction</name>
  <files>src/hooks/useStreamingChat.ts, src/hooks/useCoverageState.ts</files>
  <action>
1. **Create `src/hooks/useCoverageState.ts`**:
   - Query `api.conversationState.getCoverageState` with sessionId + currentPhase
   - Query `api.conversationState.getSearchedSources` with sessionId + currentPhase
   - Query `api.conversationState.getResearchIntensity` with sessionId
   - Compute coverage progress: count topics at each depth level, calculate percentage (deep=100%, moderate=66%, surface=33%, not_mentioned=0%)
   - Return `{ coverageState, searchedSources, researchIntensity, coverageProgress, topicDepths }`
   - `topicDepths` is array of `{ key: string; label: string; depth: string }` for UI display

2. **Update `useStreamingChat`** — Major changes to the sendMessage flow:

   a. **Add new parameters**: The hook needs access to additional context. Add parameters or accept them via the hook args:
      - Accept `isFirstSession`, `isNewSession`, `otherSessionNames` as additional args to the hook
      - Use the useCoverageState hook internally (or accept coverage data as props)
      - Import `inferEnergyLevel` from conversationState.ts

   b. **Build expanded system prompt** — Before calling the chat action, build the full context:
      ```typescript
      const energyLevel = inferEnergyLevel(messages.slice(-5));
      const systemPromptContext = {
        currentPhase,
        summaries: currentSummaries.filter(s => s.phase < currentPhase),
        sessionPath,
        isFirstSession,
        isNewSession: messages.length === 0,
        otherSessionNames,
        coverageState: coverageData?.coverageState || null,
        energyLevel,
        researchIntensity: coverageData?.researchIntensity || "medium",
        searchedSources: coverageData?.searchedSources || [],
        researchFindings: (researchFindings || []).map(f => ({ source: f.source, query: f.query })),
      };
      let systemPrompt = buildSystemPrompt(systemPromptContext);
      ```

   c. **Use chatWithResearch for ALL phases** — Remove the `isResearchPhase` gate. Always use `chatWithResearch` action (which has the tool loop). Pass the `phase` arg:
      ```typescript
      const response = await streamWithRetry(() =>
        chatWithResearch({
          sessionId,
          phase: currentPhase,
          systemPrompt,
          messages: formattedMessages,
        })
      );
      ```
      This means research tools are available in ALL phases per LOCKED decision.

   d. **Post-turn coverage extraction** — After saving the assistant response, fire the extraction action (non-blocking):
      ```typescript
      // Fire-and-forget: don't await, don't block the conversation
      extractCoverage({
        sessionId,
        phase: currentPhase,
        recentMessages: formattedMessages.slice(-10),
      }).catch(err => console.warn("Coverage extraction failed:", err));
      ```
      Import `extractCoverage` via `useAction(api.conversationActions.extractCoverage)`.

   e. **Remove the separate researchPrompt addition** — Delete the `getResearchPromptAddition` call and the `isResearchPhase` gating. The system prompt now includes research guidance for all phases.

   f. **Keep the checklist detection** — The `parseChecklistType` check stays (it's a UI trigger, not a conversation design concern).

3. **Update the hook's dependency array** to include the new data sources.

4. **Handle the `phase` arg for chatWithResearch** — The existing `chatWithResearch` action needs the `phase` arg (added in Plan 03). If the action signature hasn't been deployed yet, this will fail at runtime until Plan 03 is deployed. The TypeScript types from `convex/_generated/api.d.ts` will reflect the new signature after `npx convex dev`.
  </action>
  <verify>
    `npx tsc --noEmit` passes. The hook compiles with the expanded context. Manual test: start a conversation, verify assistant response is saved (existing behavior preserved). Check browser console for coverage extraction call (fire-and-forget).
  </verify>
  <done>
    useStreamingChat passes full conversation context to system prompt, uses chatWithResearch for all phases, fires coverage extraction after each turn, and removes the phases-0-2-only research gating.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced progress bar + research intensity control</name>
  <files>src/components/PhaseProgressBar.tsx, src/components/PhaseSegment.tsx, src/components/ResearchIntensityControl.tsx</files>
  <action>
1. **Create `src/components/ResearchIntensityControl.tsx`**:
   - Small toggle/selector with 3 levels: Low, Medium, High
   - Uses `useMutation(api.conversationState.setResearchIntensity)` to persist changes
   - Styled as a subtle control (not prominent): small pill buttons or segmented control
   - Tooltip on hover explaining each level:
     - Low: "Only flag major unsupported claims"
     - Medium: "Flag claims, competitors, and pain points"
     - High: "Flag everything including assumptions"
   - Current selection highlighted
   - Compact: fits inline in the progress bar area or chat header
   - Per LOCKED: "Setting accessible conversationally AND via UI control"

2. **Update `src/components/PhaseProgressBar.tsx`**:
   - Accept new props: `coverageTopics: Array<{ key: string; label: string; depth: string }>` and `researchIntensity` and `onIntensityChange`
   - Below the phase segments row, add a sub-topic coverage row for the CURRENT phase only:
     - Show each topic as a small dot/circle with depth indicator:
       - not_mentioned: empty circle (outline only)
       - surface: quarter-filled circle or light gray
       - moderate: half-filled or medium gray
       - deep: fully filled or dark
     - Use simple Tailwind classes: `w-2 h-2 rounded-full` with background colors
     - Tooltip on each dot shows: "{label}: {depth level}"
   - Per LOCKED: "Coverage map visible to user in the progress bar"
   - Per LOCKED: "Progress bar updates in real-time as Claude detects coverage"
   - Add the ResearchIntensityControl inline, right-aligned in the progress bar area
   - Keep existing phase segment row unchanged

3. **Update `src/components/PhaseSegment.tsx`** (if needed):
   - The progress fill within the current phase segment should now be driven by coverage percentage instead of the old progress value
   - Coverage percentage = average depth across all topics (deep=100, moderate=66, surface=33, not_mentioned=0) / number of topics
   - This makes the progress bar segment fill reflect actual conversation coverage, not the old heuristic

4. **Wire in the parent component** (Chat.tsx or wherever PhaseProgressBar is rendered):
   - Pass coverageTopics from useCoverageState hook
   - Pass researchIntensity and onIntensityChange handler
   - This may require reading Chat.tsx to find where PhaseProgressBar is rendered and threading the new props
  </action>
  <verify>
    `npx tsc --noEmit` passes. Visual check: progress bar renders without errors. Sub-topic dots appear below phase segments. Research intensity control renders with 3 options.
  </verify>
  <done>
    Progress bar shows sub-topic coverage dots for current phase. Research intensity control allows changing between low/medium/high. Coverage progress drives the phase segment fill percentage. All renders without errors.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx convex dev --once` succeeds
- Start a new exploration session: first response should include journey framing (roadmap of 4 phases)
- Send a message: coverage extraction fires (check Convex dashboard logs)
- Progress bar shows sub-topic dots below phase segments
- Research intensity control changes value and persists
- chatWithResearch used for all phases (not just 0-2)
- Research suggestions appear in conversation when triggers detected
</verification>

<success_criteria>
- End-to-end flow works: message sent -> system prompt has full context -> response generated -> coverage extracted -> progress bar updates
- Journey framing appears in first response of new session
- Research triggers work in phases 3+ (not just 0-2)
- Sub-topic coverage visible in progress bar
- Research intensity changeable via UI
- No regression: existing chat functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-conversation-design/08-04-SUMMARY.md`
</output>
