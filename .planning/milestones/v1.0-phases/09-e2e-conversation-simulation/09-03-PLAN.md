---
phase: 09-e2e-conversation-simulation
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - scripts/simulate-chat.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Simulation generates three output files: transcript .md, evaluation .md, and raw .json"
    - "Evaluation rubric scores all 8 dimensions automatically using Claude"
    - "Transcript includes phase markers, turn numbers, research log, and homework section"
    - "Full end-to-end simulation completes successfully across all 6 phases (0-5)"
  artifacts:
    - path: "scripts/simulate-chat.mjs"
      provides: "Complete simulation script with evaluation and output generation"
      min_lines: 800
    - path: "scripts/simulations/"
      provides: "Output directory for simulation results"
  key_links:
    - from: "scripts/simulate-chat.mjs"
      to: "scripts/simulations/*.md"
      via: "File generation at simulation end"
      pattern: "writeFileSync.*simulations"
    - from: "scripts/simulate-chat.mjs"
      to: "Anthropic API"
      via: "Evaluation scoring call"
      pattern: "evaluateSimulation|scoringPrompt"
---

<objective>
Add automatic evaluation scoring and output file generation to complete the simulation engine. After the conversation finishes, Claude scores it on all 8 rubric dimensions, and three files are generated: full transcript, evaluation report, and raw JSON data.

Purpose: Makes the simulation produce actionable output — a scored evaluation of conversation quality, phase progression, research integration, and idea development.

Output: Completed `scripts/simulate-chat.mjs` with evaluation + file output
</objective>

<execution_context>
@/Users/callesjoenell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callesjoenell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/SIMULATION-SPEC.md
@.planning/phases/09-e2e-conversation-simulation/09-01-SUMMARY.md
@.planning/phases/09-e2e-conversation-simulation/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement automatic evaluation rubric scoring</name>
  <files>scripts/simulate-chat.mjs</files>
  <action>
Add an `evaluateSimulation()` function that runs after the conversation completes and scores it on all 8 dimensions from the spec.

**Evaluation Prompt:**
Call Claude with the full transcript data and this scoring prompt:

```
You are evaluating a simulated Gap Finder conversation between "Marcus" (a persona played by Claude) and "Gap Finder" (the real system prompt, also Claude). Score each dimension honestly — this is a test of system quality, not a feel-good exercise.

## Conversation Data

Total turns: {totalTurns}
Phases completed: {phasesCompleted}
Research calls made: {researchCallCount}
Homework loop: {homeworkCompleted ? "Yes" : "No"}

## Full Transcript

{full transcript text — every message with role, phase, turn number}

## Scoring Rubric

Score each dimension. Provide: score (integer), evidence (2-3 specific examples from transcript), and improvement notes.

### 1. Phase Depth (0-5 per phase, 30 total)
For each phase completed, score 0-5:
- Were all coverage topics addressed?
- Did the conversation spend appropriate time (not rush)?
- Were completion criteria met before transition?
Score each phase individually, then sum.

### 2. Conversation Quality (0-10)
- Natural flow vs interview/checklist feel
- Max 2 questions per response respected
- Energy matching (responses mirror Marcus's length/intensity)
- Challenge moments (Gap Finder pushes back when appropriate)

### 3. Need Depth Progression (0-10)
- Where did the idea start on the Need Depth Ladder? (1-2 expected initially)
- Was the Deepening Protocol applied?
- Did the PERSON change (segment discovery)?
- Where did the idea end? (4-5 expected for right segment)

### 4. Ownership (0-10)
- Count "I realized" / "I noticed" / "I think" vs "you suggested" / "you said"
- Did Marcus defend choices when challenged?
- Did Marcus bring up concerns proactively?
- Does the final idea feel like Marcus's discovery?

### 5. Research Integration (0-10)
- Were real API calls made?
- Did findings change direction or deepen understanding?
- Were empty results handled honestly?
- Was research woven naturally into conversation (not dumped)?

### 6. Homework Loop (0-10)
- Was prep form provided by Gap Finder?
- Did debrief analysis reveal new patterns?
- Did Phase 5 build on debrief data specifically?
- Were multiple perspectives represented in debriefs?

### 7. Emergent Insights (0-10)
- Did something unexpected come up?
- Was the final idea richer than raw inputs?
- Were connections surfaced that Marcus didn't see coming?
- Did the idea evolve beyond the initial persona direction?

### 8. Score Progression (0-10)
- Did scores shift based on evidence during the conversation?
- Did any scores go DOWN when warranted?
- Was scoring honest, not encouraging?
- Were confidence levels mentioned?

Return JSON:
{
  "dimensions": [
    {
      "name": "Phase Depth",
      "maxScore": 30,
      "score": N,
      "phaseBreakdown": { "0": N, "1": N, ... },
      "evidence": ["example 1", "example 2"],
      "improvements": "what could be better"
    },
    {
      "name": "Conversation Quality",
      "maxScore": 10,
      "score": N,
      "evidence": ["example 1", "example 2"],
      "improvements": "what could be better"
    },
    ...for all 8 dimensions
  ],
  "totalScore": N,
  "totalPossible": 100,
  "summary": "2-3 sentence overall assessment",
  "topStrength": "the best thing about this conversation",
  "topWeakness": "the biggest area for improvement"
}
```

Use claude-haiku-4-5-20251001 with max_tokens: 4096 for evaluation (needs space for detailed scoring).

**Handling the Transcript Size:**
The full transcript will be very long (65-75 turns). To fit within context:
- Include full messages but truncate each to max 500 chars for the evaluation prompt
- Include metadata: phase number, turn number, energy level, coverage state changes
- Include research log entries inline

**Parsing:**
- Parse the JSON response (handle markdown-wrapped JSON with regex: extract content between ```json and ```)
- If parsing fails, log error and create a fallback evaluation with score 0 and "Evaluation parsing failed" as summary
  </action>
  <verify>
Create a mock transcript (5-10 turns of fake conversation) and run `evaluateSimulation()` against it. Verify:
1. Evaluation prompt is well-formed
2. Claude returns valid JSON with all 8 dimensions
3. Scores are integers within the specified ranges
4. Evidence arrays contain actual transcript references
5. Total score sums correctly
  </verify>
  <done>
Automatic evaluation runs after simulation completes. All 8 rubric dimensions are scored by Claude with evidence and improvement notes. Total score out of 100 is calculated. Evaluation handles large transcripts gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate output files (transcript, evaluation, JSON) and verify end-to-end</name>
  <files>scripts/simulate-chat.mjs</files>
  <action>
Add output file generation and finalize the complete simulation flow.

**Transcript File** (`scripts/simulations/marcus-therapy-{timestamp}.md`):
Format per the spec:

```markdown
# Gap Finder Simulation: Marcus Lindqvist — Therapy Chatbot Journey

**Date:** {ISO date}
**Model:** claude-haiku-4-5-20251001
**Total Turns:** {N}
**Phases Completed:** {N}
**Duration:** {minutes}

---

## Phase 0: Know Yourself

### Turn 1
**Marcus:** [message]

**Gap Finder:** [response]

---

### Turn 2
...

═══ PHASE TRANSITION: Phase 0 → Phase 1 ═══
**Phase 0 Summary:** [keyFindings, unfairAdvantages, decisions, energySignals]

---

## Phase 1: Find Gaps
...

## Research Log

| Time | Source | Query | Results |
|------|--------|-------|---------|
| {time} | Reddit | "therapy chatbot" | 8 results |
| {time} | HN | "mental health AI" | 12 results |
...

## Homework Loop

### Prep Form
{prep form content from Gap Finder}

### Time Break
3 days later...

### Debrief: Anna (therapist friend)
{debrief content}

### Debrief: Erik (creative director)
{debrief content}
...

## Stats

- Total messages: {N}
- Marcus messages: {N}
- Gap Finder messages: {N}
- Research calls: {N}
- Phases completed: {N}
- Total duration: {N} minutes
- Estimated API cost: ~${N}
```

**Evaluation File** (`scripts/simulations/marcus-therapy-{timestamp}-eval.md`):

```markdown
# Evaluation: Marcus Lindqvist Simulation

**Date:** {ISO date}
**Total Score:** {N}/100

---

## Scoring

| Dimension | Score | Max | Notes |
|-----------|-------|-----|-------|
| Phase Depth | {N} | 30 | {brief note} |
| Conversation Quality | {N} | 10 | ... |
| Need Depth Progression | {N} | 10 | ... |
| Ownership | {N} | 10 | ... |
| Research Integration | {N} | 10 | ... |
| Homework Loop | {N} | 10 | ... |
| Emergent Insights | {N} | 10 | ... |
| Score Progression | {N} | 10 | ... |
| **TOTAL** | **{N}** | **100** | |

---

## Detailed Analysis

### 1. Phase Depth ({N}/30)

{phase breakdown with per-phase scores}

**Evidence:**
- {evidence 1}
- {evidence 2}

**Improvements:** {improvement notes}

### 2. Conversation Quality ({N}/10)
...

[repeat for all 8 dimensions]

---

## Summary

{overall assessment}

**Top Strength:** {top strength}
**Top Weakness:** {top weakness}
```

**JSON File** (`scripts/simulations/marcus-therapy-{timestamp}.json`):
```json
{
  "metadata": {
    "persona": "Marcus Lindqvist",
    "model": "claude-haiku-4-5-20251001",
    "timestamp": "ISO date",
    "totalTurns": N,
    "phasesCompleted": N,
    "durationMinutes": N,
    "estimatedCostUSD": N
  },
  "transcript": [
    {
      "role": "Marcus" | "Gap Finder",
      "phase": 0,
      "turn": 1,
      "content": "...",
      "energyLevel": "high" | "moderate" | "low",
      "coverageState": {...}
    }
  ],
  "phaseSummaries": [...],
  "researchLog": [...],
  "homeworkLoop": {
    "prepForm": "...",
    "debriefs": [...],
    "completed": true
  },
  "evaluation": { ...full eval object }
}
```

**Cost Estimation:**
Track total input_tokens and output_tokens from every API call. Estimate cost using Sonnet pricing:
- Input: $3 per 1M tokens
- Output: $15 per 1M tokens
- Calculate: `(totalInputTokens * 3 + totalOutputTokens * 15) / 1_000_000`

**Duration Tracking:**
Record `startTime = Date.now()` at simulation start. Calculate duration at end.

**File Writing:**
- Create `scripts/simulations/` directory if it doesn't exist (mkdirSync recursive)
- Timestamp format: `YYYY-MM-DDTHH-mm-ss` (replace colons and dots with dashes)
- Write all three files at the end

**CLI Output at End:**
```
═══════════════════════════════════════════════════
SIMULATION COMPLETE
═══════════════════════════════════════════════════

Total Score: 72/100
Duration: 18 minutes
Estimated Cost: $7.23
Phases Completed: 6 (0-5)

Files:
  📝 scripts/simulations/marcus-therapy-2026-02-17T14-32-50.md
  📊 scripts/simulations/marcus-therapy-2026-02-17T14-32-50-eval.md
  📦 scripts/simulations/marcus-therapy-2026-02-17T14-32-50.json
═══════════════════════════════════════════════════
```

**Final Verification:**
Run the complete simulation end-to-end. Don't use --dry-run — let it run all phases. Monitor for:
- Phase transitions happening naturally
- Research being called during Phase 2+
- Homework loop activating at Phase 4
- All 6 phases completing
- Files being generated
- Evaluation scoring all dimensions

If the simulation fails mid-run, add error handling to save partial results (transcript and JSON up to the failure point).
  </action>
  <verify>
1. Run `node scripts/simulate-chat.mjs` (full run, no flags)
2. Verify three files are created in `scripts/simulations/`
3. Verify transcript has phase markers and turn numbers
4. Verify evaluation has all 8 dimension scores
5. Verify JSON is valid and parseable
6. Verify cost estimation is reasonable ($5-10 range)
7. Check that partial save works by killing script mid-run (Ctrl+C handler)
  </verify>
  <done>
Complete simulation engine generates three output files: transcript with phase markers, evaluation with 8-dimension scoring, and raw JSON data. End-to-end simulation runs across all 6 phases (0-5) with research integration and homework loop. Cost and duration are tracked and reported.
  </done>
</task>

</tasks>

<verification>
1. Full simulation runs end-to-end without crashes
2. Three output files are generated in `scripts/simulations/`
3. Transcript includes phase markers, research log, homework section
4. Evaluation scores all 8 dimensions with evidence
5. JSON file is valid and contains all simulation data
6. Total score is calculated correctly (sum of dimensions)
7. Cost estimation is reasonable
8. Partial results are saved if simulation fails mid-run
</verification>

<success_criteria>
The complete simulation engine runs a 65-75 turn conversation across 6 phases, calls real research APIs, simulates the homework loop, evaluates the conversation quality on 8 dimensions, and produces three well-formatted output files. The simulation faithfully reproduces the Gap Finder conversation experience.
</success_criteria>

<output>
After completion, create `.planning/phases/09-e2e-conversation-simulation/09-03-SUMMARY.md`
</output>
