---
phase: 13-about-contact-faq-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/PublicPageLayout.tsx
  - src/components/pages/AboutPage.tsx
  - src/components/pages/ContactPage.tsx
  - src/components/pages/FAQPage.tsx
  - src/App.tsx
  - src/components/WelcomePage.tsx
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03, PAGE-04]

must_haves:
  truths:
    - "Visiting /about shows the About page without requiring sign-in"
    - "Visiting /contact shows the Contact page without requiring sign-in"
    - "Visiting /faq shows the FAQ page with expandable questions without requiring sign-in"
    - "Navigation between public pages works without full page reload"
    - "WelcomePage has footer links to About, FAQ, and Contact"
    - "Each page has a document title set (e.g. About | Start Building Now)"
  artifacts:
    - path: "src/components/layout/PublicPageLayout.tsx"
      provides: "Shared header/footer wrapper for public pages"
      exports: ["PublicPageLayout"]
    - path: "src/components/pages/AboutPage.tsx"
      provides: "About page content"
      exports: ["AboutPage"]
    - path: "src/components/pages/ContactPage.tsx"
      provides: "Contact page with mailto link"
      exports: ["ContactPage"]
    - path: "src/components/pages/FAQPage.tsx"
      provides: "FAQ page with accordion items"
      exports: ["FAQPage"]
    - path: "src/App.tsx"
      provides: "Public routes before catch-all"
      contains: "Route path=\"/about\""
    - path: "src/components/WelcomePage.tsx"
      provides: "Footer nav links to public pages"
      contains: "Link to=\"/about\""
  key_links:
    - from: "src/App.tsx"
      to: "src/components/pages/AboutPage.tsx"
      via: "Route element prop"
      pattern: "Route path=\"/about\" element=\\{<AboutPage"
    - from: "src/components/pages/AboutPage.tsx"
      to: "src/components/layout/PublicPageLayout.tsx"
      via: "Layout wrapper"
      pattern: "<PublicPageLayout>"
    - from: "src/components/WelcomePage.tsx"
      to: "/about, /faq, /contact"
      via: "Link components in footer"
      pattern: "Link to=\"/about\""
---

<objective>
Add public About, Contact, and FAQ pages accessible without authentication.

Purpose: Prospective users landing on the site need trust-building content (who built this, how to reach you, common questions) before signing up. These pages must render outside the Clerk auth gate.
Output: Three public page components, a shared layout wrapper, updated routing in App.tsx, and footer links on WelcomePage.
</objective>

<execution_context>
@/Users/callesjoenell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callesjoenell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-about-contact-faq-pages/13-RESEARCH.md

@src/App.tsx
@src/components/WelcomePage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PublicPageLayout and all three page components</name>
  <files>
    src/components/layout/PublicPageLayout.tsx,
    src/components/pages/AboutPage.tsx,
    src/components/pages/ContactPage.tsx,
    src/components/pages/FAQPage.tsx
  </files>
  <action>
Create 4 new files. No new dependencies needed — use react-router-dom Link and Tailwind only.

**PublicPageLayout.tsx** — Shared wrapper for all public pages:
- Header with "Start Building Now" link to "/" (left) and nav links to /about, /faq, /contact (right)
- `children` slot for page content
- Footer with "Back to Start Building Now" link
- Style: `min-h-screen bg-gray-50 flex flex-col`, header `bg-white border-b border-gray-100`
- Import Link from react-router-dom. Zero Clerk imports.
- Follow the exact pattern from 13-RESEARCH.md Pattern 2.

**AboutPage.tsx** — About page:
- Wrap in PublicPageLayout
- Set document.title to "About | Start Building Now" via useEffect
- Content sections (use white card pattern: `bg-white rounded-2xl shadow-sm border border-gray-100 p-8`):
  1. Hero heading "About Start Building Now" with subtitle about the mission
  2. "What We Do" section: AI-guided conversations that help people discover what to build and validate their ideas (Explore path + Evaluate path)
  3. "How It Works" section: Brief 3-step explanation (Start a conversation, AI guides discovery, Get actionable insights)
  4. "Why We Built This" section: Brief founder motivation paragraph
- Max width: `max-w-3xl mx-auto px-6 py-16`
- All content is placeholder but realistic — user can edit copy post-merge

**ContactPage.tsx** — Contact page:
- Wrap in PublicPageLayout
- Set document.title to "Contact | Start Building Now" via useEffect
- Simple layout: heading, paragraph, mailto button
- Email: `mailto:hello@startbuildingnow.com` (add `{/* TODO: replace with actual email */}` comment)
- Button style: `bg-primary-500 text-white rounded-xl font-medium hover:bg-primary-600`
- Follow the exact pattern from 13-RESEARCH.md Contact Page example

**FAQPage.tsx** — FAQ page with accordion:
- Wrap in PublicPageLayout
- Set document.title to "FAQ | Start Building Now" via useEffect
- Create a local FAQItem component using useState toggle (Pattern 3 from research)
- Include 6-8 FAQ items covering:
  - What is Start Building Now?
  - How does the Explore path work?
  - How does the Evaluate path work?
  - Is it free? (mention free tier: 1 Explore + 1 Evaluate, then paid)
  - How is pricing determined? (mention weekly doubling from launch)
  - Is my data private/secure?
  - Can I come back to my conversation later? (yes, persistent)
  - Who built this?
- Each FAQItem: border-b, py-4, button with question text + plus/minus toggle, conditionally rendered answer paragraph

CRITICAL: None of these files should import anything from Clerk (@clerk/clerk-react) or Convex. They are purely public, static pages.
  </action>
  <verify>
    <automated>cd /Users/callesjoenell/Documents/GapFinder && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All four files exist, export their components, compile without TypeScript errors, and contain zero Clerk/Convex imports</done>
</task>

<task type="auto">
  <name>Task 2: Wire public routes in App.tsx and add WelcomePage footer links</name>
  <files>
    src/App.tsx,
    src/components/WelcomePage.tsx
  </files>
  <action>
**App.tsx changes:**
- Add imports for AboutPage, ContactPage, FAQPage from their new paths
- In the Routes block inside App(), add three Route elements BEFORE the `/*` catch-all:
  ```
  <Route path="/about" element={<AboutPage />} />
  <Route path="/contact" element={<ContactPage />} />
  <Route path="/faq" element={<FAQPage />} />
  <Route path="/*" element={<AuthenticatedApp />} />
  ```
- React Router v7 matches more-specific routes first, so these will resolve before the catch-all. No `exact` prop needed (removed in v6+).

**WelcomePage.tsx changes:**
- Add `import { Link } from "react-router-dom";` at the top
- Add a footer section at the very bottom of the component, after the closing `</section>` of the auth section and before the closing `</div>` of the root container:
  ```tsx
  <footer className="py-8 text-center text-sm text-gray-400 space-x-6">
    <Link to="/about" className="hover:text-gray-600">About</Link>
    <Link to="/faq" className="hover:text-gray-600">FAQ</Link>
    <Link to="/contact" className="hover:text-gray-600">Contact</Link>
  </footer>
  ```
- Use Link (not `<a>`) for client-side navigation without full page reload.
  </action>
  <verify>
    <automated>cd /Users/callesjoenell/Documents/GapFinder && npx tsc --noEmit 2>&1 | head -30 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /about, /contact, /faq routes render their respective pages without auth
    - WelcomePage shows footer links to all three pages
    - Full build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx vite build` — production build succeeds
3. Manual: visit /about, /contact, /faq in browser without being signed in — all three pages render
4. Manual: click nav links in public page header — navigation works without full reload
5. Manual: on WelcomePage, footer shows About/FAQ/Contact links
</verification>

<success_criteria>
- Three public pages (About, Contact, FAQ) accessible at /about, /contact, /faq without authentication
- Shared PublicPageLayout provides consistent navigation header and footer across all public pages
- FAQ page has expandable/collapsible question items
- Contact page has mailto link for reaching the team
- WelcomePage has footer links to all three public pages
- No Clerk or Convex imports in any public page component
- TypeScript compiles and Vite build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-about-contact-faq-pages/13-01-SUMMARY.md`
</output>
