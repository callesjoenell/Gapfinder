---
phase: 05-idea-card
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/idea-card/IdeaCard.tsx
  - src/components/idea-card/IdeaCardContent.tsx
  - src/components/idea-card/BlobBackground.tsx
  - src/components/idea-card/TestingControls.tsx
  - src/components/Chat.tsx
  - src/fixtures/phaseStates.ts
  - convex/sessions.ts
autonomous: false

must_haves:
  truths:
    - "User sees card in top 25% of screen (desktop) or 40% (mobile)"
    - "User can collapse card to thin strip showing phase indicator"
    - "Card transitions from orange to dark green when score threshold met"
    - "Developer can step through phases with mock data via testing mode"
    - "Card is integrated into main Chat view"
  artifacts:
    - path: "src/components/idea-card/TestingControls.tsx"
      provides: "Dev mode navigation for testing visual states"
      min_lines: 40
    - path: "src/fixtures/phaseStates.ts"
      provides: "Mock data for each phase state"
      exports: ["MOCK_PHASE_STATES"]
    - path: "src/components/Chat.tsx"
      provides: "IdeaCard integrated into chat layout"
      contains: "<IdeaCard"
  key_links:
    - from: "src/components/Chat.tsx"
      to: "src/components/idea-card/IdeaCard.tsx"
      via: "renders IdeaCard above MessageList"
      pattern: "<IdeaCard"
    - from: "src/components/idea-card/IdeaCard.tsx"
      to: "convex/sessions.ts"
      via: "reads ideaCardScore for color transition"
      pattern: "ideaCardScore"
---

<objective>
Complete the Idea Card integration with responsive layout, color transitions based on score, and testing mode for development verification.

Purpose: This finalizes the Idea Card feature by integrating it into the main chat experience, implementing the score-based color transition (orange to dark green) that gives users feedback on their idea quality, and providing developers with tools to verify all visual states.

Output: Fully integrated IdeaCard in Chat, color transitions, responsive collapse behavior, and testing mode with mock data.
</objective>

<execution_context>
@/Users/callesjoenell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/callesjoenell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-idea-card/05-CONTEXT.md
@.planning/phases/05-idea-card/05-RESEARCH.md
@.planning/phases/05-idea-card/05-01-SUMMARY.md
@.planning/phases/05-idea-card/05-02-SUMMARY.md
@src/components/Chat.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement color transitions and score integration</name>
  <files>
    src/components/idea-card/IdeaCard.tsx
    src/components/idea-card/IdeaCardContent.tsx
    src/components/idea-card/BlobBackground.tsx
    convex/sessions.ts
  </files>
  <action>
Color scheme from CONTEXT.md:
- Default: Orange spectrum (when no score or low score)
- Threshold: Average score >= 4 (of 5 per area) triggers green
- Binary transition: Orange until threshold, then smooth shift to dark green over 2-3 seconds

Update `convex/sessions.ts`:
- Add query `getSessionScore`:
  - Args: `{ sessionId }`
  - Returns `{ score: number | null, passesThreshold: boolean }`
  - Threshold: score >= 20 out of 30 max (6 areas x 5 points = 30, need avg 4 = 24, but use 20 for some flexibility)

Update `BlobBackground.tsx`:
- Add prop: `colorScheme: 'orange' | 'green'`
- Update gradient colors based on colorScheme:
  - Orange: #FFE5B4 to #FFA500 spectrum
  - Green: #90EE90 to #228B22 (light green to forest green)
- Animate gradient transition over 2.5 seconds when colorScheme changes
- Use `<motion.linearGradient>` or CSS custom properties for smooth color shift

Update `IdeaCardContent.tsx`:
- Add prop: `colorScheme: 'orange' | 'green'`
- When green:
  - Background transitions to dark green (#1a4d1a)
  - Text color transitions to white
  - Supporting sentence borders use lighter green shades
- Transition duration 2-3 seconds, eased

Update `IdeaCard.tsx`:
- Add `useQuery(api.sessions.getSessionScore, { sessionId })`
- Compute colorScheme based on passesThreshold
- Pass colorScheme to BlobBackground and IdeaCardContent
- When score changes and crosses threshold, trigger smooth color transition
  </action>
  <verify>
TypeScript compilation: `npx tsc --noEmit`
`grep -l "colorScheme" src/components/idea-card/*.tsx` finds all updated files
  </verify>
  <done>
- Score query returns whether threshold is met
- Blob colors transition from orange to green spectrum
- Card content background transitions to dark green with white text
- Transition is smooth (2-3 seconds) and GPU-accelerated
  </done>
</task>

<task type="auto">
  <name>Task 2: Create testing mode with mock data and navigation</name>
  <files>
    src/fixtures/phaseStates.ts
    src/components/idea-card/TestingControls.tsx
    src/components/idea-card/IdeaCard.tsx
  </files>
  <action>
Create `src/fixtures/phaseStates.ts`:
- Export `MOCK_PHASE_STATES` array with entries for phases 0-9
- Each entry contains:
  - phase: number
  - ideaKeywords: mock keywords per area (6-14 words each, vary by phase)
  - ideaSentence: null for phases 0-2, string for phase 3+
  - supportingSentences: empty for phases 0-2, 3-4 sentences for phase 3+
  - score: null for phases 0-6, number 15-30 for phases 7-9
- Phase 7 mock: score 18 (below threshold, stays orange)
- Phase 8 mock: score 24 (above threshold, turns green)
- Phase 9 mock: score 28 (well above threshold, dark green)

Example mock idea sentence: "A subscription service that connects busy professionals with local artisan food makers for personalized weekly meal deliveries"

Create `src/components/idea-card/TestingControls.tsx`:
- Only renders when URL has `?testMode=true` parameter
- Props: `{ currentTestPhase: number; onPhaseChange: (phase: number) => void }`
- Render:
  - Fixed position at bottom-left of card area
  - Left arrow button: decrement phase (min 0)
  - Phase number display: "Test Phase: {N}"
  - Right arrow button: increment phase (max 9)
  - Small, semi-transparent styling so it doesn't obstruct card view
  - Keyboard shortcuts: Left/Right arrow keys

Update `IdeaCard.tsx`:
- Add `const isTestMode = new URLSearchParams(window.location.search).get('testMode') === 'true'`
- Add `const [testPhase, setTestPhase] = useState(0)`
- When isTestMode:
  - Use MOCK_PHASE_STATES[testPhase] instead of live Convex data
  - Override currentPhase with testPhase
  - Render TestingControls
- When not isTestMode:
  - Use live Convex queries as before
  - Don't render TestingControls
  </action>
  <verify>
TypeScript compilation: `npx tsc --noEmit`
Visit `http://localhost:5173/?testMode=true` shows testing controls
Arrow keys step through phases 0-9
  </verify>
  <done>
- Mock data exists for all 10 phases with varying keywords, ideas, scores
- TestingControls renders with forward/back navigation
- Test mode activated via URL parameter
- Keyboard shortcuts work for navigation
- Can visually verify all phase states without running full conversation
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate IdeaCard into Chat layout</name>
  <files>
    src/components/Chat.tsx
    src/components/idea-card/IdeaCard.tsx
  </files>
  <action>
Update `Chat.tsx`:
- Import `IdeaCard` from './idea-card'
- Render IdeaCard above PhaseProgressBar:
```tsx
<div className="flex flex-col h-full bg-gray-50 relative">
  <IdeaCard sessionId={sessionId} currentPhase={currentPhase} />
  <PhaseProgressBar ... />
  ...
</div>
```
- IdeaCard takes its own height (25vh desktop, 40vh mobile)
- Remaining space flows to MessageList

Responsive behavior verification:
- Desktop (md+): Card is 25vh, leaves 75vh for chat
- Mobile (<md): Card is 40vh, leaves 60vh for chat
- Collapsed: Card is h-16 (64px), maximizes chat space

Update `IdeaCard.tsx`:
- Ensure collapse state persists in localStorage: `useLocalStorage('ideaCard-collapsed', false)`
- Collapsed strip shows:
  - Current phase indicator (number or name)
  - Small expand chevron icon
  - Subtle gradient hint of current color scheme

Accessibility:
- Collapse button has aria-label: "Collapse idea card" / "Expand idea card"
- Testing controls have aria-labels
- Color transitions don't rely solely on color (text changes too)
  </action>
  <verify>
`npm run build` succeeds
Visual check: IdeaCard renders above chat messages
Collapse/expand works and persists across page refresh
Mobile vs desktop height is correct (use browser devtools responsive mode)
  </verify>
  <done>
- IdeaCard integrated into Chat component layout
- Responsive heights work (25vh desktop, 40vh mobile)
- Collapse state persists in localStorage
- Collapsed strip shows phase indicator
- Accessibility attributes present
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Idea Card feature with blobs, words, merge animation, color transitions, and testing mode</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:5173/?testMode=true
3. Verify blob visualization:
   - Phase 0: 6 faint blobs with very blurred edges
   - Use arrow keys to step through phases
4. Verify word appearance:
   - Phase 1-2: Words appear within blobs, mixed sizes
   - Words should look like fuzzy word cloud
5. Verify merge animation:
   - Phase 3: Blobs move to center, words fade, idea sentence appears
   - Should feel smooth, not janky
6. Verify color transition:
   - Phase 7 (score 18): Card stays orange
   - Phase 8 (score 24): Card transitions to dark green with white text
   - Transition should be smooth (2-3 seconds)
7. Verify responsive layout:
   - Desktop: Card takes top 25% of screen
   - Mobile (use devtools): Card takes top 40%
8. Verify collapse:
   - Click collapse button: Card shrinks to thin strip
   - Refresh page: Collapse state persists
9. Verify accessibility:
   - Enable prefers-reduced-motion in browser settings
   - Animations should be disabled or minimal
10. Remove ?testMode=true and verify real session data works
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Overall plan verification:
1. `npm run build` completes without errors
2. IdeaCard visible in Chat layout
3. Testing mode allows stepping through all 10 phases
4. Color transitions work correctly based on score threshold
5. Responsive layout correct on desktop and mobile
6. Collapse/expand works with persistence
7. Accessibility: reduced motion respected, proper aria-labels
</verification>

<success_criteria>
1. IdeaCard integrated into Chat, renders above messages
2. Responsive: 25% desktop, 40% mobile height
3. Collapsible with persistent state
4. Orange-to-green color transition at score threshold
5. Testing mode with mock data for all phases
6. Keyboard navigation in test mode (arrow keys)
7. Human verification confirms visual quality and animation smoothness
</success_criteria>

<output>
After completion, create `.planning/phases/05-idea-card/05-03-SUMMARY.md`
</output>
